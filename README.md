# project1
NumPy and Pandas practice project covering array creation, reshaping, slicing, Boolean masking, vectorized operations, DataFrame creation, data cleaning, filtering, missing values, grouping, aggregation, and debugging common NumPy and Pandas errors.
